{
  "name": "@emailflare/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "migrations:apply:local": "wrangler d1 migrations apply rondosync-email --local",
    "migrations:apply": "wrangler d1 migrations apply rondosync-email --remote",
    "cf-typegen": "wrangler types",

    "cf:setup": "node ../../scripts/setup.mjs",

    "cf:build-admin": "cd ../admin && pnpm install --frozen-lockfile --ignore-workspace && pnpm --ignore-workspace run build",
    "cf:update": "pnpm run cf:build-admin && wrangler d1 migrations apply rondosync-email --remote && wrangler deploy",

    "cf:rollout": "pnpm run cf:build-admin && wrangler d1 migrations apply rondosync-email --remote && wrangler versions upload",
    "cf:rollout:full": "wrangler versions deploy --yes"
  },
  "dependencies": {
    "@emailflare/emails": "file:../emails",
    "@hono/zod-validator": "^0.4.3",
    "hono": "^4.7.5",
    "jose": "^5.10.0",
    "nanoid": "^5.1.5",
    "zod": "^3.24.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250501.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.0.0"
  }
}
